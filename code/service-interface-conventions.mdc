---
description: 定义服务接口的编码标准，包括接口设计、方法定义和业务逻辑规范。
globs: **/src/main/java/**/service/**Service.java
---
- 服务接口必须遵循业务逻辑的单一职责原则。
- 服务接口名必须以 Service 结尾。
- 必须为所有方法提供完整的 JavaDoc 注释。
- 方法名必须使用驼峰命名法并清晰表达业务意图。
- 必须使用统一的响应包装类、实体类、自定义DTO类作为返回值类型。
- 必须为方法参数提供适当的类型定义。
- 必须考虑接口的可扩展性和维护性。
- 接口方法必须体现清晰的业务边界。