# /create-service 命令

## 命令格式
```bash
/create-service [一段描述性的话]
```

## 功能说明
创建完整的服务层代码，包括服务接口、服务实现、业务逻辑封装、事务管理配置和异常处理机制。

## AI分析流程

### 1. 智能描述解析
- **提取关键信息**：从用户描述中提取模块名称、业务功能、业务逻辑
- **识别业务类型**：判断是CRUD操作、业务处理、数据转换等
- **推断业务复杂度**：评估业务的复杂度和实现难度

### 2. 智能文档发现
- **自动扫描**：扫描项目根目录下的`docs/`目录中的业务文档、需求文档、设计文档
- **内容匹配**：基于描述内容匹配相关业务文档
- **信息提取**：从文档中提取业务需求、业务规则、处理流程等
- **文件显示**：在AI响应中显示找到的相关设计文件名，便于用户确认

### 3. 基于项目结构的智能推断
- **项目分析**：分析现有项目的业务架构、服务设计
- **模块识别**：识别相关模块和业务依赖
- **规范推断**：推断项目的业务设计规范

### 4. 服务层规则匹配
- **规范匹配**：根据需求匹配对应的服务层编码规范
- **设计模式**：应用业务设计模式和最佳实践
- **代码生成**：生成符合项目规范的服务层代码

### 5. 主动询问机制
- **业务补全**：识别缺失或模糊的业务需求，主动询问用户
- **逻辑确认**：确认业务逻辑和处理流程
- **多轮对话**：支持多轮对话，逐步完善业务设计

## 使用示例

### 1. 简单描述
```bash
/create-service 用户管理服务
```

### 2. 详细描述
```bash
/create-service 用户管理服务，包含用户注册、登录验证、权限管理、个人信息管理
```

### 3. 业务场景描述
```bash
/create-service 订单管理服务，包含订单创建、支付处理、状态更新、物流跟踪
```

### 4. 技术需求描述
```bash
/create-service 商品管理服务，包含商品信息、库存管理、价格计算、分类管理
```

## AI交互示例

### 示例1：简单描述
```bash
# 用户输入
/create-service 用户管理服务

# AI响应
AI: 已解析您的描述"用户管理服务"，并发现以下相关文档：

**发现的设计文档：**
- `docs/user-business-logic.md` - 用户管理业务逻辑文档
- `docs/service-design.md` - 服务层设计文档
- `docs/business-rules.md` - 业务规则文档

基于这些文档，UserService将包含以下业务方法设计：

**业务方法设计：**
- createUser() - 创建用户
- updateUser() - 更新用户信息
- deleteUserById() - 删除用户
- getUserById() - 根据ID查询用户
- listUsers() - 用户列表查询

**需要确认的信息：**
基于您的描述，我需要确认以下信息：

1. 具体的业务处理逻辑
2. CRUD操作的具体需求
3. 权限验证的具体要求

请提供详细信息，或者确认我的推断是否正确？
```

### 示例2：详细描述
```bash
# 用户输入
/create-service 用户管理服务，包含用户注册、登录验证、权限管理、个人信息管理

# AI响应
AI: 已解析您的详细描述，并发现以下相关文档，UserService将包含完整的业务方法：

**发现的设计文档：**
- `docs/user-business-logic.md` - 用户管理业务逻辑文档
- `docs/service-design.md` - 服务层设计文档
- `docs/business-rules.md` - 业务规则文档

**业务方法实现：**
- registerUser() - 用户注册
- loginUser() - 用户登录验证
- updateUserInfo() - 更新个人信息
- manageUserPermission() - 权限管理
- getUserProfile() - 获取用户信息

**推断的业务逻辑：**
- 用户注册：验证用户名唯一性、密码加密、发送验证邮件
- 用户登录：验证用户名密码、生成JWT Token、记录登录日志
- 权限管理：角色分配、权限验证、权限更新
- 个人信息：信息更新、头像上传、密码修改

**推断的依赖关系：**
- UserMapper - 用户数据访问
- RoleMapper - 角色数据访问
- PasswordEncoder - 密码加密
- JwtUtil - JWT Token处理

**需要确认的信息：**
基于您的详细描述，我发现以下信息：

✅ **已明确**：用户注册、登录、权限管理、个人信息管理功能
✅ **业务逻辑**：密码加密、JWT认证、角色权限控制
✅ **技术栈**：Spring Boot + MyBatis + Spring Security

信息完整，直接开始生成用户管理Service类
```

## 生成内容

### 1. 服务接口 (Service Interface)
- 接口名以`Service`结尾
- 定义业务方法签名
- 清晰的业务边界划分
- 完整的JavaDoc注释

### 2. 服务实现 (ServiceImpl)
- 实现类名以`ServiceImpl`结尾
- 使用`@Service`注解标记
- 业务逻辑具体实现
- 事务管理配置

### 3. 业务方法
- 根据业务描述生成相应方法
- 标准的CRUD业务操作
- 复杂业务逻辑封装
- 数据校验和业务规则

### 4. 依赖注入
- 使用`@Autowired`注入依赖
- 数据访问层依赖注入
- 工具类和配置类依赖

### 5. 异常处理
- 业务异常处理和转换
- 数据访问异常处理
- 自定义业务异常抛出

## 标准业务方法

### 1. 基础CRUD操作
- `create{Domain}()` - 创建业务对象
- `update{Domain}()` - 更新业务对象
- `delete{Domain}ById()` - 删除业务对象
- `get{Domain}ById()` - 根据ID查询
- `list{Domain}s()` - 列表查询

### 2. 复杂业务操作
- `process{Domain}()` - 复杂业务处理
- `validate{Domain}()` - 业务数据校验
- `convert{Domain}()` - 数据转换处理

### 3. 业务查询方法
- `find{Domain}sByCondition()` - 条件查询
- `search{Domain}s()` - 搜索查询
- `get{Domain}sByPage()` - 分页查询

## 服务层特性
1. **业务封装**: 完整的业务逻辑封装
2. **事务管理**: 合理的事务边界控制
3. **异常处理**: 统一的异常处理机制
4. **可测试**: 支持单元测试和集成测试
5. **可扩展**: 支持业务功能扩展

## 技术实现
- **接口设计**: 清晰的业务接口定义
- **实现类**: 使用`@Service`注解标记
- **事务**: 使用`@Transactional`管理事务
- **依赖注入**: 使用`@Autowired`注入依赖
- **异常处理**: 业务异常和系统异常处理

## 注意事项
1. AI会根据描述自动推断业务方法
2. 自动处理事务管理和异常处理
3. 包含完整的业务逻辑和数据校验
4. 遵循项目的编码规范和架构设计
5. 生成的代码可以直接运行和测试
6. 支持多轮对话完善需求
7. 支持文档引用和智能推断
8. 支持自然语言描述

## 最佳实践建议

### 1. 描述技巧
- **具体描述**: 提供详细的业务功能描述，AI能更好地理解需求
- **关键功能**: 包含核心业务逻辑，帮助AI生成准确代码
- **业务规则**: 指定业务规则和处理流程

### 2. 交互技巧
- **分步确认**: 先确认核心业务，再完善业务规则
- **及时反馈**: 对AI的推断及时提供反馈
- **迭代优化**: 基于生成结果进行迭代优化

### 3. 文档使用
- **业务文档**: 在项目中维护业务文档，AI会自动发现
- **需求文档**: 维护需求文档，AI会参考生成业务逻辑
- **规则文档**: 维护业务规则文档，AI会参考生成验证逻辑